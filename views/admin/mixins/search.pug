mixin search(keyword)
    .box-search 
        form(
            action="/admin/search"
            method="GET"
        )
            div(class="form-group d-flex mb-6")
                input(
                    type="text"
                    placeholder="Nhập từ khóa..."
                    class= "form-control"
                    name="keyword"
                    value=keyword
                )
                button(
                    type="submit"
                    class="btn btn-primary"
                ) Tìm